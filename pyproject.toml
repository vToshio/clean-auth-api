[project]
name = "auth-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "vToshio", email = "vtoshio2004@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
auth-api = "auth_api:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
