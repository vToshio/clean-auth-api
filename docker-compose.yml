services:
  api:
    build: 
      context: .
      dockerfile: .docker/Dockerfile
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - .:/app
      - /app/.venv
    ports:
      - 8000:8000
    networks:
      - app-network

networks:
  app-network:
    driver: bridge